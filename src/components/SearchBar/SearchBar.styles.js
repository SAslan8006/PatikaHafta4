import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        padding: 8,
        margin: 5,
        borderRadius: 5,
        backgroundColor: "#eceff1",

    },
    searchTitle: {
        color: "black",
        borderWidth: 1,
        borderColor: "grey",
        borderRadius:5,

    }
});